# Permanently bypass peer dependency checks
legacy-peer-deps=true

# Use exact versions by default (no ^ or ~)
save-exact=true

# Suppress engine warnings (old Angular packages work fine with newer Node)
engine-strict=false

# Reduce npm noise
fund=false
audit=false
