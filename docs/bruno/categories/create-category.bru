meta {
  name: Create Category
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Graphics Cards",
    "slug": "graphics-cards",
    "description": "GPU and video cards"
  }
}
