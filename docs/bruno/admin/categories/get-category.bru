meta {
  name: Get Category
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/categories/1
  body: none
  auth: inherit
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
