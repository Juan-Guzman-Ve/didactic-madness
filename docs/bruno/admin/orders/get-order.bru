meta {
  name: Get Order
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/orders/1
  body: none
  auth: inherit
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
