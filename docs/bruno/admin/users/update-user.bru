meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/admin/users/1
  body: json
  auth: inherit
}

body:json {
  {
    "firstName": "Jane",
    "lastName": "Doe",
    "phone": "+1234567890",
    "status": "Active"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
