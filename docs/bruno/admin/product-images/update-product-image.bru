meta {
  name: Update Product Image
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/admin/product-images/1
  body: json
  auth: inherit
}

body:json {
  {
    "displayOrder": 2
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
