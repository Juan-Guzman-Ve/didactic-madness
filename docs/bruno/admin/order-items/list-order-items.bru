meta {
  name: List Order Items
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/order-items
  body: none
  auth: inherit
}

params:query {
  ~page: 1
  ~limit: 20
  ~sort: createdAt:desc
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Response has data array", function() {
    expect(res.body.data).to.be.an('array');
  });
}
