meta {
  name: Delete Address
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/addresses/1
  body: none
  auth: inherit
}

tests {
  test("Status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
