meta {
  name: Get Cart
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/cart
  body: none
  auth: inherit
}

params:query {
  ~page: 1
  ~limit: 50
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
